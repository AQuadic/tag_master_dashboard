import ProfileHeader from "@/components/createprofile/ProfileHeader"

const CreateProfile = () => {
    return (
        <section>
            <ProfileHeader />
        </section>
    )
}

export default CreateProfile
