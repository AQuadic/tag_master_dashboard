import ProfileTable from "@/components/createprofile/ProfileTable";

const CreateProfile = () => {
  return (
    <section>
      <ProfileTable />
    </section>
  );
};

export default CreateProfile;
